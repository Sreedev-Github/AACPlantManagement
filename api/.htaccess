RewriteEngine On

# Serve existing files (e.g. uploads)
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Route all API requests to index.php
RewriteRule ^ index.php [QSA,L]
